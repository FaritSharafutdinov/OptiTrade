# OptiTrade
TradingBot that use ML algorithms in trading
---

# 🚀 Текущий прогресс проекта Agent-Trader

На этом этапе выполнена базовая интеграция backend-сервиса, сервиса модели и подготовлена инфраструктура для подключения фронтенда.

---

## ✅ 1. Реализован Backend API-Hub

Создан основной backend-сервис на FastAPI, который выступает единым API для фронтенда и внутренних модулей.

### Основные эндпоинты:

* **/bot/status** — текущие метрики бота (баланс, прибыль/убыток, открытые позиции).
* **/trades** — история сделок.
* **/bot/start**, **/bot/stop** — запуск и остановка торгового агента.
* **/bot/update-config** — обновление конфигурации бота.
* **/model/predict** — прокси-эндпоинт, пересылающий запросы в сервис модели.
* **/health** — проверка работоспособности сервиса.

Backend также принимает записи сделок и обновляет состояние бота.

---

## ✅ 2. Создан отдельный сервис модели

Отдельный FastAPI-сервис (`model-service`) отвечает за вычисление торговых действий на основе входных рыночных данных.

На текущий момент используется простая детерминированная логика, но структура сервиса полностью готова к последующему подключению реальной модели.

---

## ✅ 3. Настроена простая база данных

По умолчанию используется SQLite (можно заменить на PostgreSQL).

Созданы таблицы:

* **trades** — история всех исполненных операций
* **bot_configs** — сохранение конфигураций бота
* **backtests** — результаты тестов и параметры запуска

Все таблицы автоматически создаются при запуске backend-сервиса.

---

# ▶️ Как запускать

### 📌 Требования:

* Python 3.9+
* Установленные зависимости (`pip install -r requirements.txt`)

---

## 1. Запуск сервиса модели

В одном терминале выполните:

```bash
uvicorn model_service.main:app --host 127.0.0.1 --port 8001 --reload
```

После запуска сервис будет доступен по адресу:

```
http://127.0.0.1:8001
```

---

## 2. Запуск backend-сервиса

Во втором терминале:

```bash
uvicorn backend.main:app --host 127.0.0.1 --port 8000 --reload
```

Backend будет доступен по адресу:

```
http://127.0.0.1:8000
```

Проверить, что сервис работает:

```
http://127.0.0.1:8000/health
```

---

## 3. Результат

После запуска двух сервисов:

* фронтенд может подключаться к backend по порту **8000**
* backend общается с сервисом модели по внутреннему API
* все данные пишутся в локальную базу данных
